printf 'GET /data/index1.html HTTP/1.0\r\nHost: example.com\r\n\r\n' | nc 127.0.0.1 3490  /// gets you the page: index1.html

printf 'GET /data/ HTTP/1.0\r\nHost: example.com\r\n\r\n' | nc 127.0.0.1 3490               // gets you the first index that matches fomr the ones that had been declared in the location of the config 

printf 'GET /uploads/ HTTP/1.0\r\nHost: example.com\r\n\r\n' | nc 127.0.0.1 3490            // gets you the audotindex (if location allows), in case that no index file has been declared in the location

printf 'GET /cgi/test.py HTTP/1.0\r\nHost: example.com\r\n\r\n' | nc 127.0.0.1 3490         // gets you the cgi response of the file requested, if the file is a cgi file and in a cgi location

printf 'DELETE /uploads/a HTTP/1.0\r\nHost: example.com\r\n\r\n' | nc 127.0.0.1 3490        // deletes file 'a' if allowed in this location

printf 'DELETE /uploads/ HTTP/1.0\r\nHost: example.com\r\n\r\n' | nc 127.0.0.1 3490        // deletes the folder 'uploads' if allowed in this location

printf 'POST / HTTP/1.0\r\nHost: example.com\r\n\r\nprint ("Heeloollool")\r\n\r\n' | nc 127.0.0.1 3490 // posts file to upload_path == ./servers/server1/uplodas (currently)

printf 'POST /a HTTP/1.0\r\nHost: example.com\r\n\r\nprint ("Heeloollool")\r\n\r\n' | nc 127.0.0.1 3490 // overwrites the file 'a' if 'a' exists and if its allowed

== POST runs cgi, if the location he posts the files in allows cgi execution, also when overwriting a file == same syntax is used ==

